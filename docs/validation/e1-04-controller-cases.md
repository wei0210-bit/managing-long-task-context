# E1-04 主控补充反例（运行前冻结）

2026-09-15T10:25:00Z；沿用 E1-04 执行合同，源码只读。独立调用公开 gate，复用 Agent 的隔离数据夹具但不导入其 TestCase。实际结果另写主控报告，不覆盖本预期。

1. C01：三开关开启，真实依据完整，prepare/activate 均通过；实际 completion 通过。随后不给独立验收 resolver，completion 必须阻断，不撤回既成接管事实。
2. C02：三开关开启且接管成功，省略真实 rule_runtime，即使业务证据及独立验收齐全也不得完成；恢复 runtime 后可以通过。
3. C03：三开关开启且接管成功，提交空 evidence_map，必须阻断，不能由测试夹具补齐；正常证据恢复后可以通过。
4. C04：Truth Sources 开启，在 prepare 前通过公开 API 标脏，完整说明该现状仍可接管；未重新观察前 completion 阻断，不能因更换主控而抹掉旧门禁。

均检查 completion 只读查询不增加激活事件，代次仍为 1。正负结果属于合成本地机制，不是模型稳定性、真实宿主或 token 收益证据。
