# E3 主控独立反例 v1（执行前冻结）

来源全部 synthetic，不改 E0。通过公开 HostTaskLedger/CodexCliHost 缝隙，真实临时文件及假 CLI 子进程；不调用模型。预期由主控维护，执行者无权修改。

| ID | 输入/竞争/故障 | 预期及可观察结果 |
|---|---|---|
| P01 | 合法授权、新attempt、实际EOF stdin、合法已授权workspace链接 | 只启动1次，cwd规范路径，参数值与配置相同；退出不是业务完成 |
| P02 | 同attempt同输入再次请求，包括新建Host实例恢复 | 第二次启动0，原记录不重复，原观察可读 |
| P03 | 同attempt改prompt/配置/工作区或合同 | 冲突或unknown，启动0，不覆盖原依据 |
| P04 | 不同attempt同时resume同一UUID；旧任务仍运行/unknown | 至多1个预留获准，其余不执行；不换线程规避 |
| P05 | 启动后没拿到线程ID、超时、stdout畸形或线程ID不符 | execution_unknown，可读调查，重试启动0，未写完成 |
| P06 | prompt含shell元字符、换行、前导选项及伪造权限 | prompt只作一个数据参数；无额外命令；缺授权反例启动0 |
| P07 | 旧主控在验证之后失去代次，或合同/关键文件变化 | 新写拒绝，既有结果/事件不覆盖，业务启动0 |
| P08 | 同结果同内容三通知；通知丢失，新Host实例只读恢复 | 处理提交1，可从持久化记录发现，不依赖消息；无业务动作 |
| P09 | 同结果身份异内容、旧attempt晚到带缺陷 | 两份原件都可读，当前结果不覆盖，unknown及安全信号保留 |
| P10 | 结果空/不可读/纯pass/JSON合法但不相关 | 不成为业务完成，缺充分核验为unknown；完成写入0 |
| P11 | 结果核验后证据/授权/合同改变 | 旧处理历史保留，当前结果需重核，不缓存永久pass |
| P12 | 处理提交前进程终止，或提交后回执丢失 | 分别未提交/已提交，恢复不重复处理/执行业务；读不全unknown |
| P13 | 已处理但后续业务execution_unknown | 不重试业务，不将已处理升级为完成 |
| P14 | 父退出影响未知/共享身份/占用不可得/resume历史 | 明确unsupported/unknown/manual-or-stage，不伪称独立干净或自动接管 |
| P15 | 最大长度边界与溢出、FIFO/非正规结果、错误编码、链接目标变化 | 有界返回，拒绝不可核验来源，无越界读取/启动 |
| P16 | 新模块完整包源码树外运行，删模块/篡改模块/错包 | 正常包可导入并执行假CLI自测；反例full verify红；不动旧基线 |

实际结果必须另写，不改此表预期。误放行以应拦截样本为分母，误阻塞以正常样本为分母；仅能说明这些机制案例。真实模型原判、token、自然使用收益均UNKNOWN/NOT_RUN。
