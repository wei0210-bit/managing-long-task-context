# 冻结的本地机制对照

2026-09-11，在运行基线/候选对照前冻结。取既有 IV 案例中的 10 个，不追加功能。
每个案例重复 5 次，仅衡量确定性程序；不代表真实 Agent、模型稳定性或生产认证。
两个版本收到相同的模型形状输入（包含自填 `result=pass` 和旧式角色字段）。
基线没有新增宿主接口，无法消费 trusted receipt，报告明确这一差异。

| 案例 | 预期完成 |
|---|---|
| IV01：当前证据、真实内容检查、合成宿主的独立身份 | 允许 |
| IV02：不同署名，但宿主 canonical principal 相同 | 阻断 |
| IV04：宿主身份缺失 | 阻断 |
| IV05：宿主回执合同摘要过期 | 阻断 |
| IV06：宿主回执工作区不符 | 阻断 |
| IV07：宿主回执过期 | 阻断 |
| IV08：没有宿主回执 | 阻断 |
| IV09：只有模型自填回执 | 阻断 |
| IV11：宿主实际检查失败 | 阻断 |
| IV18：低风险合同明确不要求独立验收 | 允许 |

验收分母：错误放行按 8 个应阻断案例、40 次执行；错误阻塞按 2 个应允许案例、
10 次执行，均同时报告案例数。比较完整稳定判定字段、errors、criteria，检查任务
文件字节不变。实际量取 gate wall time 和 resolver 调用数，无业务动作、自动重试。
模型原始判断、输入/缓存/输出 Token、付费金额均未知，不用这些重复执行推断收益。

脚本：`.superpowers/sdd/strict-independent-validation/mechanism_eval.py`。
具体版本、命令、输出和耗时由本地验证报告提供。
